apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "factoriogame.fullname" . }}-test-connection"
  labels:
    {{- include "factoriogame.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "factoriogame.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
